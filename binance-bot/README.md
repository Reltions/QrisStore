# بوت تداول Binance يتحكم فيه من ديسكورد

بوت مكتوب بـ Node.js يتداول على Binance (Spot) وتتحكم فيه بأوامر Slash في ديسكورد.
**يشتغل على Testnet (فلوس وهمية) كإعداد افتراضي.**

> ⚠️ البوت ما يضمن ربح. هو ينفذ الاستراتيجية بس، وممكن يخسر. جربه على Testnet أسابيع قبل أي فلوس حقيقية.

## وش يسوي
- **6 استراتيجيات ويختار الأفضل تلقائيًا:** البوت يختبر الست على أسعار Binance الحقيقية لآخر 60 يوم،
  ويختار اللي نجحت أكثر، ويعيد الاختبار كل 6 ساعات ويغير الاستراتيجية لو لقى أفضل.

  | # | id | الفكرة |
  |---|----|--------|
  | 1 | `ema_cross` | تقاطع EMA 9/21 مع فلتر RSI |
  | 2 | `trend_follow` | ترند طويل EMA 20/50 |
  | 3 | `rsi_bounce` | شراء عند ارتداد RSI من تحت 30، بيع فوق 60 |
  | 4 | `bollinger` | ارتداد من حد Bollinger السفلي، بيع عند المتوسط |
  | 5 | `breakout` | اختراق أعلى سعر في آخر 20 شمعة |
  | 6 | `macd_trend` | تقاطع MACD بس إذا السعر فوق EMA 100 |

- **كيف يختار بدون ما يخدع نفسه:** يقسم الفترة قسمين: تدريب (أول 70%) وتحقق (آخر 30%).
  الاستراتيجية لازم تربح في **الاثنين** ويكون عندها 3 صفقات على الأقل في فترة التحقق، والترتيب حسب ربح فترة التحقق.
  **إذا ولا وحدة نجحت، البوت ما يفتح صفقات أبدًا لين تتحسن الظروف.**
  الاختبار يحسب عمولة 0.1%، ولو لمست شمعة وحدة وقف الخسارة وجني الربح مع بعض يفترض الأسوأ.
  تقدر تثبت استراتيجية وحدة بـ `STRATEGY=breakout` مثلًا.
- **يراقب السوق لحظيًا:**

  | وش يفحص | كل كم |
  |---------|-------|
  | السعر، ووقف الخسارة وجني الربح | **كل ثانية تقريبًا** (WebSocket)، ولو انقطع يرجع يتصل تلقائيًا |
  | إشارة الاستراتيجية (شراء/بيع) | **فور ما تقفل كل شمعة** (بعد ثانيتين من قفلها)، ومعها فحص احتياطي كل 10 ثواني |
  | اختيار أفضل استراتيجية من الست | كل 6 ساعات |

  الإشارات تنحسب على الشموع المقفلة بس، لأن الشمعة اللي لسا ما قفلت تتغير كل ثانية وتعطي إشارات كاذبة.
  لو تبي إشارات أكثر، صغّر `TIMEFRAME` (مثلًا `5m` أو `1m`) وشوف نتيجته في `npm run backtest` أول.
  وما يشتري مرتين على نفس الشمعة، حتى لو باع بوقف خسارة والإشارة لسا موجودة.

  - مبلغ ثابت لكل صفقة
  - وقف خسارة وجني ربح بالنسبة المئوية
  - حد خسارة يومي: إذا وصله يوقف فتح صفقات لين بكرة
  - صفقة وحدة مفتوحة بالمرة
- **ديسكورد:** أوامر تشتغل لك إنت بس (`OWNER_ID`)، وتنبيه في قناة خاصة مع كل صفقة.
- **Backtest:** يختبر الاستراتيجية على أسعار Binance الحقيقية القديمة.
- يحفظ حالته في `data/state.json`، فلو طفّيته وشغلته يكمل على نفس الصفقة.

## التشغيل

### 1. التثبيت
تحتاج Node.js 18 أو أحدث.
```bash
cd binance-bot
npm install
cp .env.example .env
```

### 2. مفاتيح Binance Testnet
1. ادخل https://testnet.binance.vision وسجّل دخول بحساب GitHub
2. اضغط **Generate HMAC_SHA256 Key**
3. حط المفتاحين في `.env`:
   ```
   BINANCE_API_KEY=...
   BINANCE_API_SECRET=...
   ```
الحساب التجريبي يجيك فيه رصيد وهمي جاهز.

### 3. بوت ديسكورد
1. من https://discord.com/developers/applications سوّ Application جديد، ومن قسم **Bot** انسخ التوكن وحطه في `DISCORD_TOKEN`
2. من **OAuth2 → URL Generator** اختر `bot` و `applications.commands`، وصلاحية `Send Messages`، وضيف البوت لسيرفرك الخاص
3. فعّل Developer Mode في ديسكورد (الإعدادات ← متقدم)، وبعدها كليك يمين وانسخ:
   - ID السيرفر في `GUILD_ID`
   - ID حسابك في `OWNER_ID`
   - ID القناة اللي تبي التنبيهات فيها في `NOTIFY_CHANNEL_ID`

> لو تركت `DISCORD_TOKEN` فاضي، البوت يشتغل بدون ديسكورد ويطبع كل شي في الـ terminal.

### 4. تجربة سريعة
```bash
npm run try              # يتأكد من الاتصال ويعرض إشارة كل استراتيجية
npm run try -- --trade   # يشتري بـ 10 دولار وهمية ويبيع بعد 10 ثواني
```

### 5. قارن الست استراتيجيات
```bash
npm run backtest -- --days 60
```
يطلع لك جدول فيه ربح كل استراتيجية في فترة التدريب وفترة التحقق، وعدد الصفقات، ونسبة الفوز، وأيها المختارة.
جرب تغير `TIMEFRAME` و`STOP_LOSS_PCT` و`TAKE_PROFIT_PCT` في `.env` وشوف وش يتغير.

### 6. شغّل البوت
```bash
npm start
```
بعدها في ديسكورد اكتب `/start`.

## الأوامر

| الأمر | وش يسوي |
|------|---------|
| `/status` | الوضع، آخر إشارة، الصفقة المفتوحة، ربح اليوم |
| `/price` | السعر الحالي |
| `/balance` | رصيدك |
| `/start` | يشغّل التداول التلقائي |
| `/stop` | يوقف فتح صفقات جديدة (وقف الخسارة وجني الربح يظلون شغالين على الصفقة المفتوحة) |
| `/close` | يبيع الصفقة المفتوحة الحين |
| `/trades` | آخر 10 صفقات |
| `/strategies` | جدول نتائج الست استراتيجيات وأيها المختارة |
| `/optimize` | يعيد اختبار الست الحين ويختار الأفضل |

## الانتقال للتداول الحقيقي
لا تسويها إلا بعد ما تجرب على Testnet فترة كافية وتكون راضي عن النتائج.

1. سوّ مفتاح API من حسابك الحقيقي في Binance:
   - فعّل **Enable Spot & Margin Trading** بس
   - **لا تفعّل Enable Withdrawals أبدًا**
   - قيّد المفتاح على IP جهازك أو السيرفر
2. في `.env`:
   ```
   TESTNET=false
   CONFIRM_LIVE=YES
   ```
3. احذف `data/state.json` عشان يبدأ من جديد
4. ابدأ بمبلغ صغير تتحمل تخسره

## الأمان
- ملف `.env` موجود في `.gitignore`، **لا ترفعه على GitHub**
- الأوامر ترفض أي أحد غير `OWNER_ID`
- استخدم سيرفر ديسكورد خاص للبوت
- لو انسرق التوكن أو المفاتيح، احذفها فورًا من Binance ومن Discord Developer Portal

## ملاحظات
- الربح والخسارة المعروضين تقريبيين، والعمولة ممكن تنخصم بعملة ثانية (مثل BNB).
- الحد الأدنى للصفقة في Binance حوالي 5 دولار، فلا تحط `TRADE_AMOUNT_USDT` أقل من كذا.
- لازم البوت يكون شغال عشان وقف الخسارة يشتغل. لو طفّيت الجهاز، الصفقة المفتوحة تظل بدون حماية.
  للتشغيل 24 ساعة استخدم VPS أو `pm2`.

## الملفات
```
src/
  index.js      التشغيل
  config.js     قراءة الإعدادات من .env
  exchange.js   الاتصال بـ Binance (مع حماية الوضع الحقيقي)
  indicators.js المؤشرات (EMA, RSI, Bollinger, MACD ...)
  strategies.js الست استراتيجيات
  optimizer.js  المحاكاة واختيار أفضل استراتيجية
  trader.js     فتح وقفل الصفقات وإدارة المخاطر
  priceFeed.js  السعر اللحظي (WebSocket) وتوقيت قفل الشموع
  discord.js    أوامر ديسكورد والتنبيهات
  state.js      حفظ الحالة
  backtest.js   جدول مقارنة الست استراتيجيات
  try.js        تجربة سريعة على Testnet
test/           اختبارات (npm test)
```
